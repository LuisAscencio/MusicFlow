import React, { Component } from "react";
import tone from "tone";
import magenta from "@magenta/music/node/core";

export class Button extends Component {
  render() {
    return (
      <div class="jumbotron">
        <h1 class="card-title">MusicFlow</h1>
      </div>
    );
  }
}

export default Button;
